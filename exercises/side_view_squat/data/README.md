# Squat repetition data

`collect_squat_data.py` creates `side_squat_reps.csv` here. Each row is one
completed, manually labelled repetition. Keep participant/session/recording IDs so
training can evaluate by group. Do not commit identifiable participant data.
